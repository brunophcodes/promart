<h2>Listado de órdenes</h2>

<% @orders.each do |order| %>
  <p>Usuario: <%= order.user_id%></p>
  <p>Comentario: <%= order.comment%></p>
  <p>Order Items: </p>
  <ul>
    <% order.order_items.each do |item| %>
      <li><%= item.quantity %> x <%= item.product.name %></li>
    <% end %>
  </ul>
<% end %>
